Tested with Python 3.8.19

Step 1

Install required pytorch packages via conda with:

conda install pytorch==1.5.1 torchvision==0.6.1 cudatoolkit=10.2 -c pytorch

This was taken from https://pytorch.org/get-started/previous-versions/

Step 2

Install scikit-learn via pip with:

pip install scikit-learn
