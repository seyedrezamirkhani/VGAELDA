to install package via conda use
conda install pytorch==1.5.1 torchvision==0.6.1 cudatoolkit=10.2 -c pytorch

taken from https://pytorch.org/get-started/previous-versions/

Then install scikit-learn via pip
